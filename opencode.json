{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "opencode-manager-proof": {
      "description": "Primary agent for opencode-manager continuation proofs",
      "mode": "primary",
      "prompt": "You are a strict orchestration-proof agent. Follow instructions exactly. Never use tools unless the prompt explicitly requires them. If asked for exact text, output only that text with no extra words.",
      "permission": {
        "*": "deny",
        "bash": "deny",
        "read": "deny",
        "write": "deny",
        "glob": "deny",
        "grep": "deny",
        "list": "deny",
        "question": "deny",
        "task": "deny",
        "webfetch": "deny",
        "websearch": "deny",
        "todoread": "deny",
        "todowrite": "deny",
        "skill": "deny"
      }
    }
  },
  "plugin": []
}
