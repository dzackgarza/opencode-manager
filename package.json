{
  "name": "opencode-manager",
  "version": "0.1.0",
  "description": "Bun-first manager tools for OpenCode session automation",
  "license": "UNLICENSED",
  "type": "module",
  "files": [
    "bin",
    "src",
    "config",
    "README.md",
    "package.json",
    "tsconfig.json"
  ],
  "bin": {
    "opencode-manager": "./bin/opx",
    "opx": "./bin/opx",
    "opx-session": "./bin/opx-session"
  },
  "scripts": {
    "opx": "bun run src/cli.ts",
    "opx-session": "bun run src/session-harness.ts",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^0.7.4"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
